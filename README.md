# AgenticHighway Homebrew Tap

Homebrew formulae for AgenticHighway CLI tools.

## Install

```bash
brew tap AgenticHighway/tap
brew install proov
```

Or in one line:

```bash
brew install AgenticHighway/tap/proov
```

## Available formulae

- `proov`
